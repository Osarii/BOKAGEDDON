VITE_API_URL=http://localhost:3001
VITE_N8N_WEBHOOK_URL=

# Google Gemini API Configuration (Profesor IA)
# NOTE: In client-side Vite SPAs, VITE_* variables are bundled and exposed in client network/code.
# For local classroom demos, place your key in .env.local (git-ignored) or paste it in the UI.
VITE_GEMINI_API_KEY=
VITE_GEMINI_MODEL=gemini-2.5-flash

